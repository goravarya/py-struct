language: python

sudo: false

cache: packages

python:
  - "3.5"

install:
  - pipenv install 
  - pipenv install --dev
  - pipenv install codecov
  - pipenv install pytest-cov

script: 
  - pytest --cov=./

after_success:
  - codecov

